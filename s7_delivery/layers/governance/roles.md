---
id: roles
layer: governance
title: Roles and permissions overrides
stage: cross_cutting
summary: Per-profile overrides of the permission table and role profiles. An action listed here replaces its default holder set entirely; an action not listed keeps the code's default. Never leaves an action with no holder, and never lets the implementer approve their own review.
---
{
  "permissions": {},
  "profiles": {}
}
